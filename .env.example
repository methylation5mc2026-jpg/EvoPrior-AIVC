# EvoPrior-AIVC optional local environment template

# Optional: point scripts to a manually prepared Norman H5AD if not using the default path.
AIVC_NORMAN_H5AD=

# Optional: set a local matplotlib cache when using isolated GEARS checks on Windows.
MPLCONFIGDIR=.tmp_mpl_gears
